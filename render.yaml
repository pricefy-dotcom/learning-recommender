services:
  - type: web
    name: learning python
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCpmmand: uvicorn api.main:app --host 0.0.0.0 --port 8000
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.2